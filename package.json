{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "predev": "test -f .env || cp .env.example .env",
    "dev": "pnpm --parallel --filter @workspace/api-server --filter @workspace/zariya run dev",
    "db:push": "pnpm --filter @workspace/db run push",
    "db:push-force": "pnpm --filter @workspace/db run push-force",
    "db:reset": "pnpm --filter @workspace/db run reset",
    "db:check": "pnpm --filter @workspace/db run check",
    "db:seed": "pnpm --filter @workspace/db run seed",
    "db:setup": "pnpm db:reset && pnpm db:push && pnpm db:seed",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
